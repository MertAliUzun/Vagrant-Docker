sudo rm /usr/local/bin/docker-compose                  #removes docker-compose
sudo apt remove docker-compose                         #removes docker-compose package if it isn't removed already
sudo apt autoremove                                    #removes leftovers